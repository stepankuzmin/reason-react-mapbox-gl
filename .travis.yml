language: node_js

cache: npm

script:
  - npm run build

before_deploy:
  - npm run build-storybook

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: storybook-static
  on:
    branch: master
